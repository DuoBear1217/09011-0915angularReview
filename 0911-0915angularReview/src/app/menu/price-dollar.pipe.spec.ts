import { PriceDollarPipe } from './price-dollar.pipe';

describe('PriceDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
